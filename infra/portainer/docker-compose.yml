services:
  portainer-3t:
    image: portainer/portainer-ce:latest
    container_name: portainer-3t
    restart: always
    ports:
      - "9001:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_3t_data:/data

volumes:
  portainer_3t_data:
